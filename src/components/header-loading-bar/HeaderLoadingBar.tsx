import React from 'react';
import styles from './HeaderLoadingBar.module.scss';

const HeaderLoadingBar: React.FC = () => (
  <div className={styles.headerLoadingBar} />
);

export default HeaderLoadingBar;
